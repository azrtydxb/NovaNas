{{/*
The Kubernetes auth enable / role-binding steps are performed inline by
novanas-openbao-init (see init-job.yaml). This file is retained as a
permanently-disabled placeholder so the template directory matches the
bootstrap scaffold and operators know where to look.

Do NOT re-enable this Job without also removing the corresponding steps
from init-job.yaml — running both concurrently causes role overwrites.
*/}}
{{- /* intentionally renders nothing */ -}}
