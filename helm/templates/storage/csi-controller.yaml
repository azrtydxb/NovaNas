{{- if .Values.storage.csi.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: novanas-storage-csi-controller
  namespace: {{ include "novanas.systemNamespace" . }}
  labels:
    {{- include "novanas.componentLabels" (dict "ctx" . "component" "storage-csi-controller") | nindent 4 }}
spec:
  replicas: {{ .Values.storage.csi.controller.replicas }}
  selector:
    matchLabels:
      {{- include "novanas.selectorLabels" (dict "ctx" . "component" "storage-csi-controller") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "novanas.selectorLabels" (dict "ctx" . "component" "storage-csi-controller") | nindent 8 }}
    spec:
      serviceAccountName: novanas-storage
      {{- include "novanas.imagePullSecrets" . | nindent 6 }}
      securityContext:
        {{- include "novanas.securityContext" . | nindent 8 }}
      containers:
        - name: csi
          image: {{ include "novanas.image" (dict "ctx" . "image" .Values.storage.csi.controller.image) }}
          imagePullPolicy: {{ include "novanas.imagePullPolicy" (dict "ctx" . "image" .Values.storage.csi.controller.image) }}
          command: ["/usr/local/bin/novanas-storage-csi"]
          # Flag set per storage/cmd/csi/main.go (#38). Controller mode
          # uses the gRPC endpoint over TCP (rather than the unix socket
          # the node plugin uses for kubelet).
          args:
            - -endpoint=tcp://0.0.0.0:9810
            - -meta-addr=novanas-storage-meta.{{ include "novanas.systemNamespace" . }}.svc:{{ .Values.storage.meta.service.grpcPort }}
          resources:
            {{- toYaml .Values.storage.csi.controller.resources | nindent 12 }}
          securityContext:
            {{- include "novanas.containerSecurityContext" . | nindent 12 }}
{{- end }}
