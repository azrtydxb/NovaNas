# Beta channel defaults. Production-like but with verbose logging.
# Observability retention will be re-introduced here once the in-house
# observability templates land (they replace kube-prometheus-stack).
api:
  env:
    NODE_ENV: production
    LOG_LEVEL: debug

alerts:
  enabled: true
