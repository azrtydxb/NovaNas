---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: hostinterfaces.novanas.io
spec:
  group: novanas.io
  names:
    categories:
    - novanas
    kind: HostInterface
    listKind: HostInterfaceList
    plural: hostinterfaces
    singular: hostinterface
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.backing
      name: Backing
      type: string
    - jsonPath: .status.link
      name: Link
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HostInterface represents a host-level IP-bearing interface.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HostInterfaceSpec defines the desired state of HostInterface.
            properties:
              addresses:
                description: Addresses is the list of IP assignments.
                items:
                  description: HostInterfaceAddress is an IP address assignment.
                  properties:
                    cidr:
                      description: Cidr is an IP/prefix (e.g. 10.0.0.2/24 or fe80::1/64).
                      minLength: 1
                      type: string
                    type:
                      description: Type selects address acquisition mode.
                      enum:
                      - static
                      - dhcp
                      - slaac
                      type: string
                  required:
                  - cidr
                  - type
                  type: object
                type: array
              backing:
                description: |-
                  Backing names the physical NIC, Bond, or Vlan this HostInterface
                  consumes.
                minLength: 1
                type: string
              dns:
                description: Dns is the list of DNS resolver IPs.
                items:
                  type: string
                type: array
              gateway:
                description: Gateway is the default gateway (optional).
                type: string
              mtu:
                description: Mtu overrides the interface MTU.
                format: int32
                minimum: 1
                type: integer
              usage:
                description: Usage declares the functional role(s) of this interface.
                items:
                  description: HostInterfaceUsage tags an interface with its functional
                    role(s).
                  enum:
                  - management
                  - storage
                  - cluster
                  - vmBridge
                  - appIngress
                  type: string
                minItems: 1
                type: array
            required:
            - backing
            - usage
            type: object
          status:
            description: HostInterfaceStatus defines observed state of HostInterface.
            properties:
              appliedConfigHash:
                description: AppliedConfigHash is the sha256 of the last-applied nmstate
                  YAML.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              effectiveAddresses:
                description: EffectiveAddresses lists the addresses actually programmed.
                items:
                  type: string
                type: array
              link:
                enum:
                - up
                - down
                type: string
              observedGeneration:
                description: ObservedGeneration tracks the generation of the last
                  reconcile.
                format: int64
                type: integer
              phase:
                enum:
                - Pending
                - Active
                - Failed
                - Reconciling
                - Ready
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
