---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: remoteaccesstunnels.novanas.io
spec:
  group: novanas.io
  names:
    categories:
    - novanas
    kind: RemoteAccessTunnel
    listKind: RemoteAccessTunnelList
    plural: remoteaccesstunnels
    singular: remoteaccesstunnel
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.endpoint.hostname
      name: Endpoint
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RemoteAccessTunnel is a persistent outbound tunnel (WireGuard, IPsec,
          Tailscale, or SD-WAN) managed by a systemd unit on the host.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RemoteAccessTunnelSpec defines the desired state of RemoteAccessTunnel.
            properties:
              auth:
                description: RemoteAccessTunnelAuth references the tunnel credentials.
                properties:
                  secret:
                    description: Secret is a structured reference to a Secret key.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  secretRef:
                    description: SecretRef names an in-namespace Secret (legacy, simple
                      form).
                    type: string
                type: object
              endpoint:
                description: RemoteAccessTunnelEndpoint is the remote peer address.
                properties:
                  hostname:
                    minLength: 1
                    type: string
                  port:
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                required:
                - hostname
                type: object
              exposes:
                items:
                  description: RemoteAccessTunnelExpose selects which workloads traverse
                    the tunnel.
                  properties:
                    app:
                      type: string
                    via:
                      enum:
                      - tunnel
                      - direct
                      type: string
                    vm:
                      type: string
                  type: object
                type: array
              type:
                description: RemoteAccessTunnelType selects the tunnel technology.
                enum:
                - sdwan
                - wireguard
                - tailscale
                type: string
            required:
            - auth
            - endpoint
            - type
            type: object
          status:
            description: RemoteAccessTunnelStatus defines observed state of RemoteAccessTunnel.
            properties:
              appliedConfigHash:
                description: AppliedConfigHash is the sha256 of the projected Tunnel
                  spec.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectedAt:
                description: ConnectedAt is the timestamp of the most recent successful
                  handshake.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration tracks the generation of the last
                  reconcile.
                format: int64
                type: integer
              phase:
                enum:
                - Pending
                - Connected
                - Disconnected
                - Failed
                - Reconciling
                - Ready
                type: string
              publicKey:
                description: PublicKey is the locally-advertised WireGuard/Tailscale
                  public key.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
