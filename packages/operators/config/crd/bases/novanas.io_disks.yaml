---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: disks.novanas.io
spec:
  group: novanas.io
  names:
    categories:
    - novanas
    kind: Disk
    listKind: DiskList
    plural: disks
    singular: disk
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.pool
      name: Pool
      type: string
    - jsonPath: .spec.role
      name: Role
      type: string
    - jsonPath: .status.sizeBytes
      name: Size
      type: integer
    - jsonPath: .status.slot
      name: Slot
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Disk represents a physical disk device with a lifecycle state
          machine.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DiskSpec defines the desired state of Disk.
            properties:
              pool:
                type: string
              role:
                description: DiskRole is either data or spare.
                enum:
                - data
                - spare
                type: string
            type: object
          status:
            description: DiskStatus defines observed state of Disk.
            properties:
              class:
                enum:
                - nvme
                - ssd
                - hdd
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              deviceClass:
                enum:
                - nvme
                - ssd
                - hdd
                type: string
              model:
                type: string
              observedGeneration:
                format: int64
                type: integer
              recentEvents:
                items:
                  description: DiskLifecycleEvent records a notable disk state transition.
                  properties:
                    actor:
                      type: string
                    fromState:
                      description: DiskState is the lifecycle state of a physical
                        disk.
                      enum:
                      - UNKNOWN
                      - IDENTIFIED
                      - ASSIGNED
                      - ACTIVE
                      - DEGRADED
                      - FAILED
                      - DRAINING
                      - REMOVABLE
                      - QUARANTINED
                      - WIPED
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    timestamp:
                      format: date-time
                      type: string
                    toState:
                      description: DiskState is the lifecycle state of a physical
                        disk.
                      enum:
                      - UNKNOWN
                      - IDENTIFIED
                      - ASSIGNED
                      - ACTIVE
                      - DEGRADED
                      - FAILED
                      - DRAINING
                      - REMOVABLE
                      - QUARANTINED
                      - WIPED
                      type: string
                    type:
                      type: string
                  required:
                  - timestamp
                  - type
                  type: object
                type: array
              serial:
                type: string
              sizeBytes:
                format: int64
                type: integer
              slot:
                type: string
              smart:
                description: SmartInfo captures SMART health readings.
                properties:
                  lastLongTest:
                    format: date-time
                    type: string
                  lastShortTest:
                    format: date-time
                    type: string
                  overallHealth:
                    enum:
                    - OK
                    - WARN
                    - WARNING
                    - FAIL
                    - FAILED
                    type: string
                  pendingSectors:
                    format: int64
                    type: integer
                  powerOnHours:
                    format: int64
                    type: integer
                  reallocatedSectors:
                    format: int64
                    type: integer
                  temperature:
                    format: int32
                    type: integer
                type: object
              state:
                description: DiskState is the lifecycle state of a physical disk.
                enum:
                - UNKNOWN
                - IDENTIFIED
                - ASSIGNED
                - ACTIVE
                - DEGRADED
                - FAILED
                - DRAINING
                - REMOVABLE
                - QUARANTINED
                - WIPED
                type: string
              wwn:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
