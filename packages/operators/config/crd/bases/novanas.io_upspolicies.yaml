---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: upspolicies.novanas.io
spec:
  group: novanas.io
  names:
    categories:
    - novanas
    kind: UpsPolicy
    listKind: UpsPolicyList
    plural: upspolicies
    singular: upspolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.integration
      name: Integration
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.onBattery
      name: OnBattery
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UpsPolicy — NUT/apcupsd integration and shutdown orchestration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UpsPolicySpec defines the desired state of UpsPolicy.
            properties:
              authSecret:
                description: |-
                  SecretRef is the NovaNas shared reference type for K8s Secrets used by
                  operator-managed CRs (UPS credentials, SMTP auth, AD join, backup
                  encryption, etc.). It mirrors packages/schemas SecretReferenceSchema.
                properties:
                  key:
                    description: Key inside the Secret. Defaults are controller-specific.
                    type: string
                  namespace:
                    description: |-
                      Namespace of the Secret. Defaults to the CR's namespace for
                      namespaced CRs, or the operator namespace for cluster-scoped CRs.
                    type: string
                  secretName:
                    minLength: 1
                    type: string
                required:
                - secretName
                type: object
              deviceName:
                type: string
              host:
                type: string
              integration:
                enum:
                - nut
                - apcupsd
                type: string
              onBattery:
                description: OnBattery is the ordered list of actions when utility
                  power fails.
                items:
                  type: string
                type: array
              onLowBattery:
                description: OnLowBattery triggers when BatteryPercent/RuntimeSeconds
                  crosses the threshold.
                items:
                  type: string
                type: array
              port:
                format: int32
                maximum: 65535
                minimum: 1
                type: integer
              thresholds:
                description: UpsThresholds encodes battery / runtime trigger points.
                properties:
                  batteryPercent:
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                  runtimeSeconds:
                    format: int32
                    minimum: 0
                    type: integer
                type: object
            required:
            - integration
            type: object
          status:
            description: UpsPolicyStatus defines observed state of UpsPolicy.
            properties:
              batteryPercent:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the generation last processed by
                  the controller.
                format: int64
                type: integer
              onBattery:
                type: boolean
              phase:
                enum:
                - Active
                - Disconnected
                - Failed
                type: string
              runtimeSeconds:
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
