---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: firewallrules.novanas.io
spec:
  group: novanas.io
  names:
    categories:
    - novanas
    kind: FirewallRule
    listKind: FirewallRuleList
    plural: firewallrules
    singular: firewallrule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.scope
      name: Scope
      type: string
    - jsonPath: .spec.direction
      name: Direction
      type: string
    - jsonPath: .spec.action
      name: Action
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FirewallRule is a single allow/deny rule programmed into nftables
          or eBPF.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FirewallRuleSpec defines the desired state of FirewallRule.
            properties:
              action:
                description: FirewallAction is the terminal disposition for a matching
                  packet.
                enum:
                - allow
                - deny
                - reject
                - log
                type: string
              destination:
                description: FirewallEndpoint is the match block for a source/destination.
                properties:
                  cidrs:
                    description: Cidrs is a list of IPv4/IPv6 CIDRs to match.
                    items:
                      type: string
                    type: array
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels matches pods by label (only meaningful when
                      scope=pod).
                    type: object
                  ports:
                    description: Ports is the list of L4 ports to match.
                    items:
                      format: int32
                      type: integer
                    type: array
                  protocol:
                    description: Protocol narrows the match to a single L4 protocol.
                    enum:
                    - tcp
                    - udp
                    - icmp
                    - any
                    type: string
                type: object
              direction:
                description: FirewallDirection selects the traffic direction.
                enum:
                - inbound
                - outbound
                type: string
              interface:
                description: Interface restricts the rule to traffic on this NIC (scope=host
                  only).
                type: string
              priority:
                description: Priority orders rules; lower runs first. Defaults to
                  1000.
                format: int32
                type: integer
              scope:
                description: FirewallScope selects the enforcement plane for a rule.
                enum:
                - host
                - pod
                type: string
              source:
                description: FirewallEndpoint is the match block for a source/destination.
                properties:
                  cidrs:
                    description: Cidrs is a list of IPv4/IPv6 CIDRs to match.
                    items:
                      type: string
                    type: array
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels matches pods by label (only meaningful when
                      scope=pod).
                    type: object
                  ports:
                    description: Ports is the list of L4 ports to match.
                    items:
                      format: int32
                      type: integer
                    type: array
                  protocol:
                    description: Protocol narrows the match to a single L4 protocol.
                    enum:
                    - tcp
                    - udp
                    - icmp
                    - any
                    type: string
                type: object
            required:
            - action
            - direction
            - scope
            type: object
          status:
            description: FirewallRuleStatus defines observed state of FirewallRule.
            properties:
              appliedConfigHash:
                description: AppliedConfigHash is the sha256 of the rendered nftables/eBPF
                  blob.
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              installedAt:
                description: InstalledAt is the timestamp of successful rule installation.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration tracks the generation of the last
                  reconcile.
                format: int64
                type: integer
              phase:
                enum:
                - Pending
                - Active
                - Failed
                - Reconciling
                - Ready
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
