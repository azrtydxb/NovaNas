---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: cloudbackuptargets.novanas.io
spec:
  group: novanas.io
  names:
    categories:
    - novanas
    kind: CloudBackupTarget
    listKind: CloudBackupTargetList
    plural: cloudbackuptargets
    singular: cloudbackuptarget
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.bucket
      name: Bucket
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.reachable
      name: Reachable
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CloudBackupTarget is an S3/B2/Azure/GCS/Swift object-storage
          endpoint.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudBackupTargetSpec defines desired state.
            properties:
              bucket:
                type: string
              credentialsSecret:
                description: |-
                  CredentialsSecret is a Secret containing provider credentials.
                  For S3/B2: AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY.
                  For Azure: AZURE_ACCOUNT_NAME / AZURE_ACCOUNT_KEY.
                properties:
                  key:
                    description: Key inside the Secret's data map.
                    type: string
                  name:
                    description: Name of the Secret.
                    type: string
                  namespace:
                    description: |-
                      Namespace of the Secret. Defaults to the channel namespace or
                      "novanas-system" when empty.
                    type: string
                required:
                - key
                - name
                type: object
              endpoint:
                description: Endpoint overrides the provider's default endpoint (S3-compat
                  only).
                type: string
              engine:
                description: CloudBackupEngine enumerates supported backup engines.
                enum:
                - restic
                - borg
                - kopia
                type: string
              prefix:
                type: string
              provider:
                description: CloudBackupProvider enumerates supported object-storage
                  providers.
                enum:
                - s3
                - b2
                - azure
                - gcs
                - swift
                type: string
              reachabilityProbeIntervalSeconds:
                description: |-
                  ReachabilityProbeIntervalSeconds controls how often the controller
                  probes the target. Default 300 when unset.
                format: int32
                type: integer
              region:
                type: string
              repositoryPasswordSecret:
                description: |-
                  RepositoryPasswordSecret is the encryption password secret for
                  restic/borg/kopia.
                properties:
                  key:
                    description: Key inside the Secret's data map.
                    type: string
                  name:
                    description: Name of the Secret.
                    type: string
                  namespace:
                    description: |-
                      Namespace of the Secret. Defaults to the channel namespace or
                      "novanas-system" when empty.
                    type: string
                required:
                - key
                - name
                type: object
              storageClass:
                description: StorageClass is the provider storage class, e.g. "STANDARD_IA".
                type: string
            required:
            - bucket
            - credentialsSecret
            - provider
            type: object
          status:
            description: CloudBackupTargetStatus reports observed state.
            properties:
              capabilities:
                description: Capabilities is the detected capability set cached at
                  probe time.
                properties:
                  lifecyclePolicy:
                    type: boolean
                  multipartUpload:
                    type: boolean
                  objectLock:
                    type: boolean
                  serverSideEncryption:
                    type: boolean
                  versioning:
                    type: boolean
                type: object
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastProbeAt:
                description: LastProbeAt is the last probe timestamp.
                format: date-time
                type: string
              lastProbeError:
                description: LastProbeError is the most recent probe error, if any.
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                enum:
                - Pending
                - Probing
                - Ready
                - Unreachable
                - Failed
                type: string
              reachable:
                description: Reachable reflects the last probe outcome.
                type: boolean
              repositoryInitialized:
                description: |-
                  RepositoryInitialized is true once the engine has initialised the
                  remote repository (e.g. `restic init`).
                type: boolean
              resolvedEndpoint:
                description: ResolvedEndpoint is the fully resolved endpoint used
                  at probe time.
                type: string
              usedBytes:
                description: UsedBytes is an informational size of all objects under
                  Prefix.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
