---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: alertchannels.novanas.io
spec:
  group: novanas.io
  names:
    categories:
    - novanas
    kind: AlertChannel
    listKind: AlertChannelList
    plural: alertchannels
    singular: alertchannel
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.consecutiveFailures
      name: Failures
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AlertChannel is an email/webhook/slack/pagerduty/ntfy/pushover
          destination.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertChannelSpec defines desired state.
            properties:
              email:
                description: EmailChannelConfig configures SMTP delivery.
                properties:
                  from:
                    type: string
                  passwordSecret:
                    description: SecretKeyRef is a reference to a key within a Secret.
                    properties:
                      key:
                        description: Key inside the Secret's data map.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                      namespace:
                        description: |-
                          Namespace of the Secret. Defaults to the channel namespace or
                          "novanas-system" when empty.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  smtpPort:
                    format: int32
                    type: integer
                  smtpServer:
                    type: string
                  startTls:
                    type: boolean
                  to:
                    items:
                      type: string
                    type: array
                  usernameSecret:
                    description: SecretKeyRef is a reference to a key within a Secret.
                    properties:
                      key:
                        description: Key inside the Secret's data map.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                      namespace:
                        description: |-
                          Namespace of the Secret. Defaults to the channel namespace or
                          "novanas-system" when empty.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - to
                type: object
              minSeverity:
                description: MinSeverity gates which alerts are dispatched on this
                  channel.
                enum:
                - info
                - warning
                - critical
                type: string
              ntfy:
                description: NtfyChannelConfig configures the ntfy.sh family of push
                  destinations.
                properties:
                  authSecret:
                    description: SecretKeyRef is a reference to a key within a Secret.
                    properties:
                      key:
                        description: Key inside the Secret's data map.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                      namespace:
                        description: |-
                          Namespace of the Secret. Defaults to the channel namespace or
                          "novanas-system" when empty.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  priority:
                    type: string
                  server:
                    type: string
                  topic:
                    type: string
                required:
                - topic
                type: object
              pagerduty:
                description: PagerDutyChannelConfig wraps the PD Events v2 routing
                  key.
                properties:
                  component:
                    type: string
                  integrationKeySecret:
                    description: SecretKeyRef is a reference to a key within a Secret.
                    properties:
                      key:
                        description: Key inside the Secret's data map.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                      namespace:
                        description: |-
                          Namespace of the Secret. Defaults to the channel namespace or
                          "novanas-system" when empty.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  severity:
                    type: string
                required:
                - integrationKeySecret
                type: object
              pushover:
                description: PushoverChannelConfig configures the Pushover API.
                properties:
                  device:
                    type: string
                  priority:
                    format: int32
                    type: integer
                  tokenSecret:
                    description: SecretKeyRef is a reference to a key within a Secret.
                    properties:
                      key:
                        description: Key inside the Secret's data map.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                      namespace:
                        description: |-
                          Namespace of the Secret. Defaults to the channel namespace or
                          "novanas-system" when empty.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  userKeySecret:
                    description: SecretKeyRef is a reference to a key within a Secret.
                    properties:
                      key:
                        description: Key inside the Secret's data map.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                      namespace:
                        description: |-
                          Namespace of the Secret. Defaults to the channel namespace or
                          "novanas-system" when empty.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - tokenSecret
                - userKeySecret
                type: object
              rateLimitPerMinute:
                description: RateLimitPerMinute bounds delivery attempts; 0 means
                  unlimited.
                format: int32
                type: integer
              slack:
                description: SlackChannelConfig wraps a Slack webhook URL secret.
                properties:
                  channel:
                    type: string
                  iconEmoji:
                    type: string
                  username:
                    type: string
                  webhookUrlSecret:
                    description: SecretKeyRef is a reference to a key within a Secret.
                    properties:
                      key:
                        description: Key inside the Secret's data map.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                      namespace:
                        description: |-
                          Namespace of the Secret. Defaults to the channel namespace or
                          "novanas-system" when empty.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                required:
                - webhookUrlSecret
                type: object
              suspended:
                description: Suspended silences the channel without deleting it.
                type: boolean
              type:
                description: AlertChannelType enumerates the supported delivery backends.
                enum:
                - email
                - webhook
                - slack
                - pagerduty
                - ntfy
                - pushover
                - discord
                - telegram
                type: string
              webhook:
                description: WebhookChannelConfig configures a generic JSON webhook
                  target.
                properties:
                  headers:
                    additionalProperties:
                      type: string
                    type: object
                  insecureSkipTlsVerify:
                    type: boolean
                  method:
                    type: string
                  secretRef:
                    description: SecretKeyRef is a reference to a key within a Secret.
                    properties:
                      key:
                        description: Key inside the Secret's data map.
                        type: string
                      name:
                        description: Name of the Secret.
                        type: string
                      namespace:
                        description: |-
                          Namespace of the Secret. Defaults to the channel namespace or
                          "novanas-system" when empty.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  timeoutSecs:
                    format: int32
                    type: integer
                  url:
                    type: string
                required:
                - url
                type: object
            required:
            - type
            type: object
          status:
            description: AlertChannelStatus reports observed state.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              consecutiveFailures:
                description: |-
                  ConsecutiveFailures counts back-to-back delivery failures;
                  resets on first success.
                format: int32
                type: integer
              deliveryCount:
                description: DeliveryCount is the total lifetime count of successful
                  deliveries.
                format: int64
                type: integer
              lastDeliveryAt:
                description: LastDeliveryAt is the time of the most recent delivery
                  attempt.
                format: date-time
                type: string
              lastError:
                description: LastError is the most recent dispatch error message.
                type: string
              lastSuccessfulDeliveryAt:
                description: LastSuccessfulDeliveryAt is the last delivery that succeeded.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation reconciled
                  last.
                format: int64
                type: integer
              phase:
                enum:
                - Pending
                - Active
                - Failed
                - Suspended
                type: string
              resolvedSecretRef:
                description: |-
                  ResolvedSecretRef echoes the generated/normalised Secret reference
                  used by the downstream dispatcher.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
