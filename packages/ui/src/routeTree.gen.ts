/* eslint-disable */
// biome-ignore-all
/**
 * Auto-generated placeholder. The real `routeTree.gen.ts` is produced by the
 * `@tanstack/router-plugin` Vite plugin at dev/build time.
 *
 * This file exists so that `tsc --noEmit` passes without first running Vite.
 * Once `pnpm dev` or `pnpm build` runs, the plugin will overwrite this file
 * with the actual generated tree.
 */

import { Route as rootRoute } from './routes/__root';

// A minimal "all routes unknown" placeholder — TanStack Router will happily
// accept this at runtime, but the canonical generated file provides strongly
// typed path params for the whole app.
export const routeTree = rootRoute;
