apiVersion: novanas.io/v1alpha1
kind: StoragePool
metadata:
  name: ssd-erasurecoded
spec:
  nodeSelector:
    matchLabels:
      novanas.io/storage-tier: ssd
  backendType: raw
  deviceFilter:
    type: ssd
    minSize: "200Gi"
