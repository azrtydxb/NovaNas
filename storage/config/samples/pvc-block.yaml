apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: example-block-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: novanas-block
  resources:
    requests:
      storage: 10Gi
